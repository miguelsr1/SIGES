hibernate:
    jdbc:
        use_get_generated_keys: true
    temp:
        use_jdbc_metadata_defaults: false    
files:
    tmp:
        path: ${env.TMP_MEDIA_PATH}
    media:
        path: ${env.MEDIA_PATH}
    lucene:
        path: ${env.LUCENE_PATH}
        
